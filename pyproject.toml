[project]
name = "evidenceiq"
version = "0.1.0"
description = "AI investigation command center with citation-backed evidence intelligence."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
